export const toAddProperty = (payload) => ({type: 'ADD_PROPERTY', payload});

export const toDeleteProperty = (payload) => ({type: 'DELETE_PROPERTY', payload});

export const toSortPropertiesByName = (payload) => ({type: 'SORT_PROPERTIES_BY_NAME', payload});

export const toSortPropertiesByType = (payload) => ({type: 'SORT_PROPERTIES_BY_TYPE', payload});
